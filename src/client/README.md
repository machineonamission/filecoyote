# client

the default client, served by the server.

dioxus abstracts away http endpoints as functions, but all communication between client and server is entirely over a
defined HTTP api, so any alternative frontends or API applications will be equally powerful