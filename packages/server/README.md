# Server crate

In order to keep the client and server maximally separate, this is a separate crate.

It won't build properly without the client crate cause dioxus is weird, but this is all the server code

plus makes module management easier